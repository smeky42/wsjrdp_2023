
= action_button(t('activerecord.button.cmt_documents'),
                '/groups/' + params[:group_id] + '/people/'+ params[:id] + '/check/cmt_documents',
                :envelope,
                remote: false,
                method: :get,
                rel: :tooltip,
                title: t('activerecord.button.status_hint'))